[build]
  command = "npm install"
  publish = "src"
  functions = "functions"

[functions]
  # .mjs files are ESM — zisi handles them natively
  # No bundler needed
  node_bundler = "zisi"
  external_node_modules = ["tls-impersonate"]
  included_files = ["functions/node_modules/**"]
